配置vim：编辑~/.vimrc
vim总是显示行号：在.vimrc中输入set number

命令模式：
	撤销：			u
	恢复撤销：		ctrl + r
	删除一个字符：		x
	剪切：			n + dd
	复制n行：		n + yy
	粘贴：			p
	快速编辑：		模式 + 动作 + 字符/常用成对符号（如{}）
					模式：
						v （块选择模式）
						d（剪切）
						y （复制）
					动作
						f（find）
						i （符号对内的块）
	
					动作
						f（find）
						i （符号对内的块）
