
使用 git notes 命令，可以为某次提交追加批注，而不会导致这个提交的 id 发生变化

》git notes add -m "批注" 提交id：添加批注，如果该提交已经存在批注，将会失败，如需覆盖，加上 -f 参数。
》git notes append -m "批注" 提交id：追加批注，如果该提交已经存在批注，将会保留原有批注的前提下，追加本次批注。
》其它命令见官方文档，或使用 git notes -h 获取完整说明